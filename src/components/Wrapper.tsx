import styled from "styled-components";
import Displays from "../Displays";

const Wrapper = styled.div`
  width: 100%;
  margin: 0 auto;
  padding-top: 215.97px;
  @media (max-width: ${Displays.tablet}) {
    padding-top: 67.01px;
  }
`;

export default Wrapper;
